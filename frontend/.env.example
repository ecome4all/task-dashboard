# Leave blank for local dev (Vite proxies /api to localhost:4000, see vite.config.ts).
# In production, set this to the deployed backend's URL, e.g. https://task-dashboard-backend.up.railway.app
VITE_API_BASE_URL=
